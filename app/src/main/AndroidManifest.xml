<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.kindeep.treachery">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:largeHeap="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".forensic.StartGameActivity" android:label="@string/start"></activity>
        <activity android:name=".player.JoinGameActivity"  android:label="@string/join"/>
        <activity android:name=".player.PlayerActivity" android:label="@string/player"/>
        <activity android:name=".forensic.ForensicActivity" android:label="@string/forensic"/>
        <activity android:name=".MainActivity" android:label="@string/main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>